<%= form_with(model: rack_config, local: true) do |form| %>
  <% if rack_config.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rack_config.errors.count, "error") %> prohibited this rack_config from being saved:</h2>

      <ul>
      <% rack_config.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :customer %>
    <%= form.text_field :customer %>
  </div>

  <div class="field">
    <%= form.label :sku %>
    <%= form.text_field :sku %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
